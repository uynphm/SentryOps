# Dashboard env (copy to .env or .env.local)
# When using the Vite dev proxy these are optional — 
# requests to /api/* are forwarded to localhost:8000 automatically.

VITE_API_URL=http://localhost:8000
VITE_API_KEY=change-me-strong-random-key
